apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stories.fullname" . }}-test-connection"
  labels:
{{ include "stories.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "stories.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

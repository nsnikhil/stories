version: '3.7'

services:

  stories:
    build:
      context: ../
      dockerfile: Dockerfile
    restart: always
    ports:
      - 8080:8080
    env_file:
      - local.env
    networks:
      - storiesnetwork
    depends_on:
      - db
      - grafana